---
description: 依 Figma 設計系統實作 UI 的規則。對應 Design System（xLwc3BIEzHKIkgEfMa3Zsm），涵蓋元件、樣式、設計 token、素材與 Figma→程式碼流程。
globs: "**/*.html,**/*.css,image/**"
alwaysApply: false
---

# Figma 設計系統規則

本專案的 Figma 設計系統來源：
- **Figma 檔案**：[Design System](https://www.figma.com/design/xLwc3BIEzHKIkgEfMa3Zsm/Design-System?node-id=4585-2)
- **File Key**：`xLwc3BIEzHKIkgEfMa3Zsm`
- **設計系統節點**：`4585-2`（對應工具參數請用 `4585:2`）

任何從 Figma 實作 UI 時，必須以該設計系統為準，並遵守以下規則。

---

## 技術棧

- **標記**：HTML5
- **樣式**：原生 CSS，使用 `:root` 的 CSS 變數（design tokens）
- **字型**：Noto Sans TC（400 / 500 / 600 / 700），已於 `index.html` 引入
- **架構**：單一或多個 HTML + 內嵌或外部 CSS，無 React/Vue

---

## 必跑流程（不可省略）

從 Figma 實作或調整 UI 時，依序執行：

1. **取得設計脈絡**：對目標節點呼叫 `get_design_context(fileKey="xLwc3BIEzHKIkgEfMa3Zsm", nodeId="...")`  
   - 若回傳過大或被截斷，先呼叫 `get_metadata` 取得節點樹，再對需要的子節點呼叫 `get_design_context`。
2. **取得視覺參考**：對同一節點呼叫 `get_screenshot(fileKey="xLwc3BIEzHKIkgEfMa3Zsm", nodeId="...")`，作為實作與驗收依據。
3. **下載素材**：依 Figma 回傳的圖片/SVG 等資源下載至專案（見下方「素材」）。
4. **實作**：依 `get_design_context` 與截圖，用本專案的 HTML/CSS 與 tokens 產出程式碼。
5. **對齊設計系統**：顏色、字級、間距等一律對應設計系統與既有 tokens，不硬寫數值。
6. **驗收**：對照 Figma 截圖檢查版面、字型、顏色、間距與互動狀態，再標記完成。

---

## 設計 Token（:root）

**IMPORTANT**：顏色、背景、文字、間距等必須使用既有 CSS 變數，不得硬編碼色碼或魔術數字。

### 調色盤與遊戲用 token（已存在於 `index.html`）

```css
--palette-white: #ffffff;
--palette-peach: #e8c5a7;
--palette-mauve: #c49a88;
--palette-olive: #b3a45d;
--palette-teal: #4e9c7c;
--game-bg: #e8dfd4;
--game-bg-warm: #d4c4b0;
--game-primary: #c85a54;
--game-primary-hover: #b34a44;
--game-accent: #6b8e6b;
--game-text: #3d3829;
--game-text-muted: #6b6355;
```

- 實作時：Figma 的顏色請對應到以上變數；若設計系統有新增 token，在 `:root` 中新增並在此規則中補上說明。
- 背景、按鈕、強調、文字主/次要色等，一律用 `var(--game-*)` 或 `var(--palette-*)`。

### 字型與排版

- 字體：`font-family: "Noto Sans TC", "Microsoft JhengHei", "PingFang TC", sans-serif;`
- 字重：400 / 500 / 600 / 700，依設計系統指定使用。
- 字級、行高、字距：依 Figma 規格實作，若設計系統有定義 token，優先使用 token。

### 間距與圓角

- 間距：與現有版面一致（如 8px、12px、24px），或依設計系統的 spacing 定義；可考慮在 `:root` 新增 `--space-*` 統一管理。
- 圓角：依設計（如 10px、12px、16px），與現有 `.chat-panel`、`#game` 等風格一致。

---

## 實作原則

- **輸出語言**：Figma MCP 若產出 React/Tailwind，僅作「設計與結構」參考，最終產出必須是 **本專案的 HTML + CSS**（內嵌或外部樣式）。
- **重用既有樣式**：先檢查 `index.html` 內已有 class（如 `.chat-tab`、`.login-btn`、`.main-layout` 等），能沿用就沿用，或擴充 class，不重複造輪。
- **元件與結構**：按設計系統的階層與語意使用標籤（如 `button`、`nav`、`section`），並保持無障礙（必要時加上 `aria-*`、`role`）。
- **視覺一致**：以 Figma 截圖為準，達成 1:1 視覺與狀態（hover、active、disabled 等）。

---

## 素材

- 圖片、圖示、SVG：以 Figma MCP 回傳的資源為準；若提供 localhost 網址，直接使用該來源，**不要**替換成其他圖庫或佔位圖。
- **不要**隨意新增 icon 套件；優先使用設計系統內或 MCP 回傳的素材。
- 存放位置：專案內 `image/`（或既有約定目錄），路徑與 `index.html` 中的引用方式一致。

---

## 檔案與結構

- 新 UI 區塊：優先加在既有 `index.html` 的適當區塊內，或依專案後續約定拆成獨立 HTML/CSS。
- Class 命名：維持現有風格（小寫、連字號），例如 `game-content`、`chat-panel`、`login-btn`。
- 新元件：若設計系統有命名，可沿用或轉成 class 名稱（如 `Card` → `.card`）。

---

## 設計系統節點參考

- 設計系統主節點（用於取得整體規範或首屏）：`node-id=4585-2` → 工具參數 `nodeId="4585:2"`。
- 實作單一元件或畫面時，使用該元件/畫面的具體 node ID（從 Figma 連結的 `node-id` 取得，並將 `-` 改為 `:` 傳入工具）。

---

## 驗收檢查

實作完成前請確認：

- [ ] 版面、對齊、間距與 Figma 一致  
- [ ] 字型、字級、字重與設計系統一致  
- [ ] 顏色皆使用 `var(--*)` token  
- [ ] 互動狀態（hover / active / disabled）已實作  
- [ ] 素材路徑正確、不依賴未授權圖庫  
- [ ] 無障礙基本要求已滿足（可聚焦、語意標籤等）
