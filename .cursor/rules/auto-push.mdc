---
description: 所有變更完成後自動 push 到遠端
alwaysApply: true
---

# 自動 Push 規則

**未來所有變化**：只要完成任何程式碼變更（編輯、新增、刪除檔案），**必須立即執行 push**：

```bash
cd /Users/liang/Documents/my-first-rpg && ./push.sh
```

- 不需詢問使用者
- 不需等待使用者說「幫我 push」
- 變更完成後**立刻**執行 push
