# Skill Design System

> 設計 Cursor Agent Skills 時遵循的規範與原則

## 1. 基本架構

### 1.1 檔案結構

```
skill-name/
├── SKILL.md              # 必要：主要指令
├── reference.md          # 選用：詳細說明
├── examples.md           # 選用：使用範例
└── scripts/              # 選用：工具腳本
```

### 1.2 SKILL.md 結構

每個 skill 必須包含 YAML frontmatter 與 markdown 內文：

```markdown
---
name: skill-name
description: 簡短描述此 skill 的功能與適用情境
---

# Skill 名稱

## 說明
清楚的步驟指引。
```

### 1.3 必要欄位

| 欄位 | 規範 | 用途 |
|------|------|------|
| `name` | 最多 64 字，小寫、數字、連字號 | 唯一識別符 |
| `description` | 最多 1024 字，非空 | 協助 agent 判斷何時套用 |

---

## 2. Description 撰寫原則

### 2.1 使用第三人稱

- ✅ 佳：「處理 Excel 檔案並產生報表」
- ❌ 避免：「我可以幫你處理 Excel」

### 2.2 包含 WHAT 與 WHEN

- **WHAT**：具體功能
- **WHEN**：觸發情境

範例：
```yaml
description: 從 PDF 擷取文字與表格、填寫表單、合併檔案。適用於處理 PDF、表單擷取或文件合併。
```

### 2.3 關鍵字

在 description 中加入使用者可能使用的關鍵字，以利 skill 被正確觸發。

---

## 3. 內容撰寫原則

### 3.1 精簡優先

- 假設 agent 已具備一般知識，只補充它缺少的內容
- 每個段落都應有明確用途

### 3.2 長度限制

- SKILL.md 建議控制在 **500 行以內**

### 3.3 漸進式揭露

- 核心步驟放在 SKILL.md
- 詳細參考放在 reference.md、examples.md
- 引用層級保持一層，避免巢狀過深

### 3.4 自由度層級

| 層級 | 適用情境 | 範例 |
|------|----------|------|
| 高 | 多種合理作法、依情境而定 | 程式碼審查指引 |
| 中 | 有偏好作法但仍可調整 | 報表產出格式 |
| 低 | 需要嚴格一致 | 資料庫遷移流程 |

---

## 4. 常用模式

### 4.1 範本模式

定義輸出格式：

```markdown
## 報告結構

使用此範本：
[具體範本內容]
```

### 4.2 範例模式

以具體範例說明用法，提升產出品質。

### 4.3 流程模式

將複雜流程拆成明確步驟與檢查清單。

### 4.4 條件分支模式

在關鍵決策點提供不同路徑指引。

### 4.5 驗證迴圈模式

對品質敏感的工作，加入驗證與修正步驟。

---

## 5. 工具腳本

預先撰寫腳本的優點：

- 比即時產生程式碼更穩定
- 節省 token
- 提高一致性与可重複性

撰寫說明時需明確指出：腳本是要**執行**還是**參考**。

---

## 6. 應避免的做法

### 6.1 路徑

- ✅ 使用：`scripts/helper.py`
- ❌ 避免：`scripts\helper.py`（Windows 風格）

### 6.2 選項過多

優先提供一個預設方案，再視情況補充例外作法。

### 6.3 時效性內容

避免寫死日期（例如「2025 年 8 月前請用舊 API」），改以版本或區塊標示新舊作法。

### 6.4 術語不一致

同一概念使用同一術語（如：一律用「API endpoint」而非混用 URL、route、path）。

### 6.5 模糊的 skill 名稱

- ✅ 佳：`processing-pdfs`、`analyzing-spreadsheets`
- ❌ 避免：`helper`、`utils`、`tools`

---

## 7. 驗證檢查清單

建立或修改 skill 時，確認：

### 核心品質

- [ ] description 具體且含關鍵字
- [ ] description 包含 WHAT 與 WHEN
- [ ] 使用第三人稱撰寫
- [ ] SKILL.md 不超過 500 行
- [ ] 術語一致
- [ ] 範例具體可執行

### 結構

- [ ] 檔案引用僅一層深度
- [ ] 使用漸進式揭露
- [ ] 流程步驟清楚
- [ ] 無明顯時效性陷阱

### 若有腳本

- [ ] 腳本能實際解決問題
- [ ] 依賴套件有說明
- [ ] 錯誤訊息清楚
- [ ] 路徑風格一致
